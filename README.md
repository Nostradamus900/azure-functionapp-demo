# Azure Function App Demo

This repository is linked to an Azure Function App via GitHub Actions (CI/CD).

Every commit to the **main** branch will trigger an automatic build and deployment to Azure.
